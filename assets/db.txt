CREATE TABLE IF NOT EXISTS trails(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, description TEXT NOT NULL, distance REAL NOT NULL, elevation_gain REAL NOT NULL, difficulty TEXT NOT NULL, latitude REAL NOT NULL, longitude REAL NOT NULL)